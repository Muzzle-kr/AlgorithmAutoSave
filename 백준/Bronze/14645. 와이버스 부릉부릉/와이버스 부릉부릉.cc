#include <iostream>
#include <vector>

using namespace std;

int main() {
    int N, K;
    cin >> N >> K;

    for (int i = 0; i < N; i++) {
        int A, B;
        cin >> A >> B;
    }

    cout << "비와이" << endl;

    return 0;
}